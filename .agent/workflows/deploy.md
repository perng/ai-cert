---
description: Deploy the Quarto book to GitHub Pages
---

1. Ensure you are in the project root.
2. Run the publish command:
   ```bash
   quarto publish gh-pages
   ```
   This command will:
   - Render the book.
   - Create/Update the `gh-pages` branch.
   - Push the content to GitHub.
